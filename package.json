{
  "name": "janapp",
  "version": "0.2.0",
  "private": true,
  "description": "JanApp — persoonlijk dashboard (feed, to-do, chat) als PWA",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "multer": "^2.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
